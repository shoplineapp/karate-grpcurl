package examples;

import com.edison42.karate.grpcurl.GrpcurlClient;
import com.intuit.karate.Results;
import com.intuit.karate.Runner;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

import java.util.Arrays;

class ExamplesTest {

    @Test
    void testParallel() {
//        Results results = Runner.path("classpath:examples")
//                //.outputCucumberJson(true)
//                .parallel(5);
//        assertEquals(0, results.getFailCount(), results.getErrorMessages());

//        GrpcurlClient grpcurlClient = new GrpcurlClient("cart-api-s.shoplinestg.com", 443);
//        String serviceList = grpcurlClient.list();
//        System.out.println(serviceList);
//
//        String result = grpcurlClient.call("cart_api_sdk.CartApi/GetCart", "{}");
//        System.out.println(result);
    }

}
